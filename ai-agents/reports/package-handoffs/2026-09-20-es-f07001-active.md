# ES-F07001 active handoff ? 2026-09-20

## Identity
- Package: `ES-F07001`
- Finding: issue #216 `R07-001`
- Claim: comment `5752623651`
- Branch: `package/es-f07001-network-presence-fence`
- Starting main: `c1054da6a8f89b312df2e05e25edc958fceda7ef`
- Migration boundary: merged V20; X03 owns V21; D09 owns V22; this package adds no migration.

## Reconciliation
Preserve X03/PR #139, D08/PR #214, D09/PR #203, D13/PR #178, T01/PR #215, and campaign fixer PRs created after issue #216 began. No live package owns `JdbcNetworkIdentityStore.java`; active Velocity fixer work is deliberately avoided. Shared registry/workspace/latest-handoff files remain under T01 ownership and are not changed here.

## Reproduced failure
Current main discovers matching source accounts with an unlocked read of `players.current_server`, then later decides whether a newly observed account may automatically inherit an active sanction. A source login can commit `current_server` after that snapshot while the joining transaction continues with stale `currentlyOnline=false`, allowing inheritance when simultaneous play should lower confidence and block automatic inheritance.

## Repair prepared
- Discover the bounded candidate set before taking player locks.
- Lock the joining player plus every matched source player in deterministic unsigned-UUID-byte order.
- Derive the decision presence from the locked row while retaining any earlier observed-online state conservatively.
- Hold those locks through relationship/evidence and sanction inheritance decisions.
- Move identity-token upsert behind the deterministic player locks for the normal decision path to avoid shared-lock/X-lock upgrade ordering hazards.
- Split the prior 107-line/CCN-18 orchestration method into focused transaction/orchestration helpers.

## Regression coverage prepared
- Stable offline single new-account evidence still inherits one eligible sanction and retry does not duplicate it.
- OFFLINE?ONLINE after the candidate query but before row locks produces simultaneous-play evidence, LOW_CONFIDENCE, and zero inherited sanction.
- ONLINE?OFFLINE in the same window remains conservatively non-inheriting.
- Opposite-direction simultaneous observations complete using deterministic player-row lock ordering.
- Existing duplicate-observation and manual-decision integration coverage remains in the same suite.

## Local evidence
- Java 21.0.6 `:persistence:compileJava :integration-tests:compileTestJava`: PASS.
- Java 21.0.6 `:persistence:test :domain:test`: PASS.
- `git diff --check`: PASS.
- Lizard changed-production bounds: new/changed methods max CCN 6, max length 35, max args 8. Remaining file warnings are pre-existing `confidence`/`validateManualChange`; the refactor removes the old `observeAndInherit` warning.
- Focused `NetworkIdentityStoreIntegrationTest` execution: NOT RUN / infrastructure failure. Testcontainers failed during class initialization because the workstation has no valid Docker environment; no test method executed and no pass is claimed.

## Remaining gates
Freeze an exact candidate head, push/open the dedicated PR, run hosted Java 21 clean build including MariaDB/Testcontainers and runtime/artifact checks as applicable, static analysis/Codacy with zero new valid findings, full review/thread reconciliation, and any policy-applicable Sentinel/Pi/staging gate. Reconcile current main and all live changed paths again immediately before merge.

## Systems not to disturb
No production/private data, no issue #43 acceptance/cutover, no LiteBans authority change, no migration renumbering, no X03/D08/D09/D13/T01 absorption, no shared canonical state overwrite while T01 owns those paths.
