# ES-F02006 active handoff — 2026-09-20

## Routing
- Finding: `R02-006` in issue #216.
- Claim: `CLAIM R02-006 by ES-F02006/package/es-f02006-case-sanction-hierarchy` (comment `5752550578`).
- Start main: `c1054da6a8f89b312df2e05e25edc958fceda7ef`.
- Branch: `package/es-f02006-case-sanction-hierarchy`.
- Status: `ACTIVE` / `ACTIONABLE_CONTINUATION`.

## Live ownership preserved at claim
- ES-T01 / PR #215: Paper random-teleport implementation/tests plus shared registry/workspace/latest-handoff files.
- ES-D08 / PR #214: cross-platform punishment integration; no `JdbcSanctionMutationStore` ownership.
- ES-D09 / PR #203: includes `SanctionChangeGuiController` and branch-local V22; this repair stays below that GUI and does not edit it.
- ES-D13 / PR #178: parked role-sync work; no case-wide sanction-store overlap.
- ES-X03 / PR #139: parked Market work and branch-local V21.
- Main migration ceiling: V20. ES-F02006 reserves no migration.
- Other review-campaign claims observed before selection included R09-002, R08-004, R01-004, and R02-004; none owns this repair path.

## Reconstructed failure
Current main locks a case row in `JdbcSanctionMutationStore.apply(...)`, but `lockCase(...)` selects only target/state/revision. The request already carries the current actor rank, while `cases.actor_rank` is persisted and the exact-sanction store already uses it with `StaffHierarchy.mayMutate(...)`. Because the case-wide store never loaded or compared that rank, otherwise-authorized lower-ranked staff could mutate higher-rank or SYSTEM-issued cases.

## Repair checkpoint
- `lockCase(...)` now reads `actor_rank` in the same `SELECT ... FOR UPDATE` used by the mutation transaction.
- Stored rank parsing mirrors the exact-sanction path: legacy `OWNER` maps to `FOUNDER`; blank/unknown values fail closed as `SYSTEM`.
- `apply(...)` rejects `HIERARCHY_DENIED` before any expiration cleanup, sanction/case mutation, event, audit, or outbox write when the existing hierarchy contract disallows the actor.
- No bypass capability is inferred or added; the case-wide request has no explicit bypass contract.
- Focused MariaDB tests cover Mod->Helper/Mod success, Mod->Admin/Founder denial, Admin->Founder denial, Founder->SYSTEM denial, repeated denied retry, authorized replay after restart, stale expectation rejection, restart re-evaluation after issuer-rank change, and a concurrent issuer-rank update held by the case row lock.

## Changed paths prepared
- `persistence/src/main/java/net/enthusia/staff/persistence/JdbcSanctionMutationStore.java`
- `integration-tests/src/test/java/net/enthusia/staff/integration/CaseSanctionHierarchyIntegrationTest.java`
- `ai-agents/work-packages/packages/ES-F02006.md`
- this handoff

## Validation state
No gate is claimed as passing yet. Exact-head focused/full Java 21 validation, applicable static analysis, changed-code complexity/method-length checks, `git diff --check`, hosted checks, Codacy zero new valid findings, and review reconciliation remain required. Runtime/artifact, Sentinel/Pi, and staging applicability must be determined from the frozen final scope and current policy.

## Collision/state publication rule
PR #215 currently owns `PACKAGE-REGISTRY.md`, `WORKSPACE-STATE.md`, and `agent-handoffs/latest.md`. ES-F02006 must not overwrite those shared records concurrently. The durable issue claim plus this unique package record/handoff preserve state until canonical publication can be serialized safely.

## Production/privacy boundary
No production moderation mutation or database operation was performed. No raw production case data, player data, credentials, or private moderation content is recorded here.

## Exact next action
Commit the prepared checkpoint as one package commit, open the PR, run exact-head validation/review, repair only valid same-finding defects, then re-reconcile live main and canonical-file ownership before any terminal publication or merge.
