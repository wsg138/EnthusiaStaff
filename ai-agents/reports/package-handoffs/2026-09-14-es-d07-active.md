# ES-D07 active handoff — 2026-09-14

Package: `ES-D07 — Discord punishment enforcement`

State: `REVIEW` / `MERGE_PENDING`.

## Live claim

- Claimed from exact `main` `074c0ae0bee7222bcd5c9096f8db0071f5b84cdf`.
- Branch: `package/es-d07-discord-punishment-enforcement`.
- PR: #201.
- No D07 implementation PR or branch existed before this claim.
- Dependencies D03, D05, and D06 are complete; D07 was the next dependency-complete READY Discord package after D13 was correctly parked.

## Concurrent-work fence

- ES-D13 PR #178 remains open/unmerged at its frozen candidate and is not touched.
- PR #199 owns the independent Paper staff-tools menu and documentation paths.
- ES-X03 PR #139 remains independent Market work; D07 does not take over or synchronize that branch.
- Main already owns the V19 Discord operational tables and V20 account-linking migration. D07 adds no Flyway migration.

## Product boundary

D07 is Discord-only enforcement. No production Discord configuration/data changes, production deployment/cutover, LiteBans authority change, AutoMod enforcement, cross-platform `Both` orchestration, or issue #43 acceptance is authorized.

## Frozen reviewed product

- Frozen executable product head: `aea6696cb97df4463b90abfcbbd8bfa4bb80b913`.
- The prior Codacy repair structurally eliminated all three valid complexity findings by grouping service construction dependencies, grouping restriction-command scope input, and splitting worker fakes into focused test fixtures.
- A later CodeRabbit pass exposed four additional valid correctness/data-integrity findings. They were verified against the current source and repaired together at `aea6696c...`:
  - reversible punishment removal now rejects invalid source states while pending/retry apply expiry uses a dedicated no-effect terminal transition;
  - custom duration parsing accepts valid values up to the existing 3650-day maximum;
  - Discord configuration IDs fail closed on signed-long overflow to match JDA String-ID parsing;
  - managed mute reconcile/removal proves current ownership from the latest relevant audit-log role change and cannot adopt a later external remove/re-add from an old D07 marker.
- Regression coverage accompanies each repair. All four original CodeRabbit threads were answered with the exact repair/evidence and resolved.
- Disposable inspection/repair workflows and scripts remain only on isolated temporary branch `tmp/es-d07-codacy-inspect-20260914`; none are part of PR #201.

## Pre-push repair validation

Review-repair workflow `34925882460`, job `104243730808`, verified exact starting head `97f6f235fbaefb4b3956d8aef7fe9e132823b682` and only pushed after all gates passed:

- Temurin Java `21.0.12+1`.
- `./gradlew --no-daemon --no-configuration-cache clean build :staff-bot:verifyStaffBotRuntime` — PASS (`BUILD SUCCESSFUL`, 69 actionable tasks; integration, domain, Paper, persistence, protocol, staff-bot, and Velocity tests/builds ran).
- Repository PMD 6.55.0 on changed Java — PASS, zero findings.
- Lizard changed-Java limits — PASS: `CCN <= 8`, practical method length `<= 50`, argument count `<= 8`.
- `DiscordPunishmentWorkerTest` bounded nonblank/non-line-comment count — 431, PASS.
- `git diff --check` — PASS.

This evidence applies to frozen executable head `aea6696c...`. It does not substitute for the normal current-head hosted PR workflows, fresh Codacy analysis, or final review state.

## Staging availability

Isolated non-production Discord enforcement remains unverified because live reconciliation found no authorized D07 destructive target/fixture/harness in the trusted staging repository. This is recorded as unavailable, not as a pass. The package contract frozen at claim requires destructive staging where such an authorized non-production harness is available; production Discord is never used as a substitute. No production Discord action was attempted.

## Current final-validation state

The GitHub-Actions-authored repair commit is followed by normal repository-authored state-only checkpoint commits. They change only package/handoff tracking and exist so every ordinary `pull_request/synchronize` gate evaluates the frozen executable product through the current PR head. Executable evidence remains attributed to `aea6696c...`; any later executable change invalidates it and requires a fresh full gate.

Required before merge:

- all current applicable hosted workflows terminal green on the final state-only head;
- fresh Codacy static analysis with zero new valid findings;
- final CodeRabbit/review inspection with zero valid unresolved threads;
- PR remains non-draft and mergeable;
- live `main` reconciliation immediately before merge, with normal merge-history synchronization/revalidation if `main` moved materially.

## Exact next action

Inspect every normal hosted check, Codacy result, CodeRabbit review/thread, and mergeability result for the final state-only checkpoint. Repair any new valid finding without weakening D07 semantics. If all required gates are clean and staging remains genuinely unavailable under the original package contract, reconcile current `main`, merge PR #201 normally, verify exact containment and safe cleanup, then publish canonical `COMPLETE` state to `main` without starting another package.
