# ES-D06 read-only staff moderation UX — MERGE_PENDING

Date: 2026-08-28
Status: `MERGE_PENDING` / `ACTIONABLE_CONTINUATION`
Package: `ES-D06 — Read-only staff moderation UX`
Starting main SHA: `500136b37c9acc30b1de8a057feb79d3d16fc400`
Implementation branch: `package/es-d06-read-only-moderation-ux`
Implementation PR: #177
Frozen merge candidate: the commit containing this record; exact SHA and hosted run identifiers are published durably on PR #177 after the gates complete.

## Selection and collision boundary

Live GitHub was reconciled before selection and throughout the continuation. D04 and D05 are complete. D06 owns the existing actionable branch/PR and is the required continuation, so no replacement PR or second Discord package was created. Unrelated provider/website/competition work, including PR #139 / ES-X03, remains outside D06 and was not overwritten, absorbed, closed, superseded, or merged. D06 adds no Flyway migration and does not steal migration/shared serialization work from another package.

## Safety boundary

Issue #43 remains outside this package. D06 is read-only: no warn/mute/kick/ban/restrict mutation, AutoMod enforcement, website mutation, production deployment, production Discord configuration, production/private player-data access, LiteBans authority change, or cutover is authorized. No Discord bot token or other secret was requested, printed, copied, logged, or committed.

## Product state

The D06 slash/context command surface, private ephemeral profile/history/linked/notes/cases views, linked-staff authority resolution, read-time authorization, signed expiring replay-resistant components, ambiguity-safe Minecraft resolution, Discord-only and Bedrock-linked handling, and fail-closed behavior are implemented with package-scoped tests.

Earlier CodeRabbit findings remain repaired: the authority bridge accepts only the exact IPv4 loopback host used by Paper, ambiguous player choices are capped at Discord's 25-option limit with overflow marked truncated, and stale package records were corrected. All previously visible inline review threads were resolved/outdated before the current repair cycle.

## Codacy/PMD blocker reconciliation

The prior blocker was not bypassed or relabeled. The worker created a temporary branch-only, no-external-secret diagnostic that ran the repository's documented PMD 6.55.0 `ruleset.xml` against every Java file changed by PR #177. Run `33144344043` / job `98761994237` exposed individual findings rather than aggregate counts: authority-endpoint resource/literal findings, controller string-builder/literal performance findings, and the custom mutable-map concurrency rule in test fakes.

The first repair addressed each class of finding directly:

- the HTTP method is named rather than repeated as a conditional literal;
- StringBuilder sizing and literal appends are explicit and bounded;
- the repeated item separator is a constant;
- `PlayerResolution.Ambiguous` uses an explicit canonical constructor rather than parameter reassignment;
- mutable map fields in D06 test fakes use `ConcurrentHashMap`;
- the authority endpoint's executor has one line-specific `NOPMD` ownership note because the executor intentionally escapes constructor scope into this `AutoCloseable`, while both startup rollback and `close()` shut it down. This is an individually documented false positive, not a broad rule exclusion.

Repair run `33144490940` / job `98762443017` completed successfully: repository PMD 6.55.0 reported zero findings and `./gradlew :domain:test :staff-bot:test :paper:test` completed `BUILD SUCCESSFUL` on Java 21. Ordinary PR Actions generated from the preceding bot-authored repair commit were `action_required` with zero jobs, so they remain explicit non-passing history rather than accepted evidence.

A later owner-authored broad branch-only Codacy Analysis CLI diagnostic at head `cd5f274813284b8432c6e128ca7a60ea64ad8cad`, run `33144915073` / job `98763712253`, exposed the remaining category rather than leaving the hosted summary opaque. It reported ten Semgrep `hard-coded-password` findings in exactly two D06 source files. Every reported line assigns only the public name of an environment variable to a configuration-key constant; no password, authority credential, component signing secret, or token value is embedded there. The actual values continue to come from environment input, secret-length validation remains intact, and `StaffModerationConfiguration.toString()` continues to redact the database and secret fields.

Candidate `e4f60d930d9e4e133ada62d97b154b65e9a20483` removed the temporary diagnostic workflow and added a generic line-local `nosemgrep` explanation to each affected constant. Exact-head Staff Bot Configuration Cache `33182644259` and Sentinel Restart Artifact `33182644279` passed; the Sentinel build job `98887490393` checked out exact head `e4f60d930d9e4e133ada62d97b154b65e9a20483`, built the Paper runtime successfully on Java 21, and published artifact `9690371328` with SHA-256 `f55a06901b6e20a09b77584e58a5a534b50ffb4a7a2ee1d0b72dc8efa839d4d7`. Those exact-head successes are retained as superseded history once the head changes.

Hosted Codacy Static Code Analysis check `98887854870` on `e4f60d930d9e4e133ada62d97b154b65e9a20483` still completed `action_required` with three new issues, down from seven on the preceding diagnostic head. It is not treated as a pass. Because the broad Codacy CLI diagnostic had isolated the analyzable issue category to the exact Semgrep rule `Semgrep_codacy.java.security.hard-coded-password`, and because generic suppression did not yield a zero-findings hosted result, the current repair makes each false-positive suppression rule-specific. Every affected declaration now has its own explanatory comment plus `nosemgrep: Semgrep_codacy.java.security.hard-coded-password`. This avoids a repository-wide exclusion and cannot suppress unrelated Semgrep findings.

The temporary diagnostic workflow is absent from the merge candidate. A later attempt to create a separate diagnostic-only workflow branch for the three remaining hosted annotations was rejected by the tool safety boundary; no workflow commit was created on that branch, and the implementation PR head was not rewritten or force-pushed. The empty diagnostic branch is cleanup-only and must be deleted after D06 containment is proven or earlier if deletion is independently safe.

The same broad diagnostic's PMD 7 adapter could not load the repository's custom PMD 6 ruleset because of an `XPathRule` compatibility difference; this is retained as diagnostic-tool incompatibility, not called a product pass or failure. Required PMD evidence remains the successful repository-native PMD 6.55.0 run above.

## Historical green evidence

Before the later Codacy inconsistency, exact product head `10b4255102489b5c423e1bb22c8daaa009fba6f9` passed Coverage `33116219168` / job `98671451083`, Staff Bot Configuration Cache `33116219157`, Sentinel Restart Artifact `33116219081`, Codacy Static Code Analysis `98671703302` with zero issues, Codacy Diff Coverage `98673935155`, and requested CodeRabbit review with no actionable findings. That history establishes the feature baseline but is not substituted for the current candidate's required exact-head gates after code changed.

At diagnostic head `cd5f274813284b8432c6e128ca7a60ea64ad8cad`, ordinary exact-head Coverage `33144917696`, Staff Bot Configuration Cache `33144917695`, and Sentinel Restart Artifact `33144917682` passed. Codacy Static Code Analysis `98763941841` remained `action_required` with seven new issues and is not relabeled as passing. Codacy Diff Coverage `98765135224` passed with a non-gating 43.01% diff-coverage result. These results belong to the diagnostic head and are superseded by later candidates.

## Merge-candidate gates

External implementation blockers: none. D06 remains `MERGE_PENDING`, not `COMPLETE`.

Before merge, the exact commit containing this record must independently satisfy all applicable gates:

- hosted Coverage/full Java 21 build/test/runtime validation;
- Staff Bot Configuration Cache;
- Sentinel Restart Artifact and canonical Sentinel restart verdict when required by the package protocol;
- hosted Codacy Static Code Analysis with zero new valid findings, plus its non-gating diff-coverage result;
- final review with no valid actionable findings;
- canonical Pi staging bound to this exact PR head, including durable public verdict and cleanup evidence;
- unchanged-current-`main` / mergeability reconciliation immediately before merge.

Any queued, missing, cancelled, `action_required`, failed, superseded, or different-head result remains non-passing. If executable state or the exact head changes, invalidated gates must be rerun.

## Exact next action

Run and reconcile all merge-candidate gates on the new frozen exact head. Obtain final review and canonical staging only against that same frozen head. If `main` advances, preserve its legitimate work with a normal merge into this branch and repeat invalidated validation. When every gate is actually passing, merge PR #177 normally (never squash/rebase/force/auto-merge), verify feature-head containment in `main`, perform safe branch cleanup, publish terminal D06 `COMPLETE` state durably, and stop without beginning D07, D13, or any second Discord package.
