# ES-D06 read-only staff moderation UX — MERGE_PENDING

Date: 2026-08-28
Status: `MERGE_PENDING` / `ACTIONABLE_CONTINUATION`
Package: `ES-D06 — Read-only staff moderation UX`
Starting main SHA: `500136b37c9acc30b1de8a057feb79d3d16fc400`
Implementation branch: `package/es-d06-read-only-moderation-ux`
Implementation PR: #177
Current repair candidate: the commit containing this record; its immutable SHA is published on PR #177 after the atomic branch update.

## Selection and collision boundary

The owner explicitly reassigned the existing D06 continuation to this worker on 2026-08-28 and instructed it to finish D06. No replacement PR or second Discord package was created. Unrelated provider/website/competition work, PR #139 / ES-X03, and PR #178 / ES-D13 remain outside D06 and are not overwritten, absorbed, closed, superseded, or merged. D06 adds no Flyway migration.

## Safety boundary

Issue #43 remains outside this package. D06 is read-only: no warn/mute/kick/ban/restrict mutation, AutoMod enforcement, website mutation, production deployment, production Discord configuration, production/private player-data access, LiteBans authority change, or cutover is authorized. No Discord bot token or other secret was requested, printed, copied, logged, or committed.

## Product state

The D06 slash/context command surface, private ephemeral profile/history/linked/notes/cases views, linked-staff authority resolution, read-time authorization, signed expiring replay-resistant components, ambiguity-safe Minecraft resolution, Discord-only and Bedrock-linked handling, and fail-closed behavior are implemented with package-scoped tests.

Earlier CodeRabbit findings remain repaired: the authority bridge accepts only the exact IPv4 loopback host used by Paper; ambiguous player choices are capped at Discord's 25-option limit with overflow marked truncated; and package state is tracked on the existing D06 branch/PR. Queue saturation receives a bounded private retry response, and unexpected read failures are privacy-safely logged by error code/type before returning a generic unavailable response.

## PMD and static-analysis reconciliation

The worker did not bypass or relabel failed evidence. The repository-native PMD 6.55.0 diagnostic run `33144344043` / job `98761994237` exposed individual findings, and repair run `33144490940` / job `98762443017` passed PMD with zero findings plus `:domain:test :staff-bot:test :paper:test` on Java 21. The authority endpoint retains one line-specific `NOPMD` ownership note because its bounded executor intentionally transfers into the `AutoCloseable` and is shut down both on startup rollback and `close()`.

A later broad Codacy CLI diagnostic at `cd5f274813284b8432c6e128ca7a60ea64ad8cad`, run `33144915073` / job `98763712253`, found ten instances of one Semgrep rule: `Semgrep_codacy.java.security.hard-coded-password`. All ten were declarations whose values are public environment-variable names, not credentials. The diagnostic PMD 7 adapter also could not load the repository's PMD 6 XPath ruleset; that tool-version incompatibility remains diagnostic-only and is not called a product pass or failure.

Hosted Codacy on candidate `e4f60d930d9e4e133ada62d97b154b65e9a20483` still returned `action_required`; exact rule-specific `nosemgrep` comments on later head `7572017f59057b7e50a75d5d6d193b71ea93fe63` also did not solve the problem. Hosted Codacy check `98891617701` on that head reported seven new issues and remains explicit non-passing evidence.

To eliminate ambiguity, the existing diagnostic branch was fast-forwarded from exact D06 source head `7572017f59057b7e50a75d5d6d193b71ea93fe63`. Diagnostic run `33188515534` / job `98907671552` completed successfully and reproduced ten local findings, all the same Semgrep hard-coded-password rule and all on environment-variable-name declarations in `DiscordStaffAuthorityEndpoint` and `StaffModerationConfiguration`. The line-specific suppression syntax was therefore proven ineffective in the current analyzer.

The terminal repair is structural rather than suppressive: constants that name environment variables are renamed from `*_KEY` to `*_ENV`, their public literal environment names remain byte-for-byte unchanged, and the ineffective Semgrep suppressions are removed. Tests are updated only for the Java identifier rename. No credential material, secret value, network endpoint, runtime configuration contract, authorization rule, or persistence behavior changes.

## Superseded exact-head evidence

The preceding exact source head `7572017f59057b7e50a75d5d6d193b71ea93fe63` passed:
- Coverage/full Java 21 build/test/runtime validation `33183626083` / job `98890856338`;
- Staff Bot Configuration Cache `33183626002` / job `98890856444`;
- durable Sentinel restart job `321` with terminal `PAPER_RESTART_OK`;
- Codacy Diff Coverage check `98893600959`, non-gating 43.01%.

Its hosted Codacy Static check `98891617701` was `action_required` with seven new issues and never passed. Because this repair changes executable Java source, all applicable executable evidence must be rerun on the new exact head; preceding passes remain superseded history only.

## Review state

Three earlier inline CodeRabbit findings are resolved/outdated: stale package records, exact IPv4 loopback authority binding, and the Discord 25-option ambiguity ceiling. One current inline finding correctly identified that the package checklist did not explicitly include the required unchanged-current-`main` / mergeability reconciliation immediately before merge. The checklist now includes that gate. The thread must be replied to/resolved after this commit is published, and final merge requires zero valid unresolved review threads.

## Merge-candidate gates

External implementation blockers: none. D06 remains `MERGE_PENDING`, not `COMPLETE`.

Before merge, the exact commit containing this record must independently satisfy:
- hosted Coverage/full Java 21 build/test/runtime validation;
- Staff Bot Configuration Cache;
- Sentinel Restart Artifact and canonical Sentinel restart verdict when applicable;
- hosted Codacy Static Code Analysis with zero new valid findings, plus non-gating diff coverage;
- harsh full-diff/final review with zero valid actionable findings;
- canonical Pi staging bound to the exact PR head, including public/private correlation and required cleanup;
- unchanged-current-`main` / mergeability reconciliation immediately before merge.

Queued, missing, cancelled, `action_required`, failed, superseded, wrong-head, or merge-ref-only results remain non-passing. Any executable-head change invalidates affected exact-head gates.

## Exact next action

Publish the atomic structural repair and this state record to the existing D06 branch; update PR #177 with the exact immutable head; rerun hosted gates and the isolated diagnostic; resolve the now-fixed package-record review thread; obtain zero-new-valid-finding Codacy and zero unresolved valid reviews; run exact-head Sentinel/canonical Pi evidence; and re-read current `main` immediately before merge. If `main` advanced, merge it normally into D06 and rerun invalidated gates. When every applicable gate is green, merge PR #177 with a normal merge commit, prove feature-head containment/no unique work, safely clean temporary branches, publish D06 `COMPLETE` durably, and stop without starting D07, D13, or any second package.
