# ES-F01004 — Cheat Tester Folia player handoff repair

## Identity and status
- Finding: `R01-004` in issue `#216`.
- Branch: `package/es-f01004-cheat-tester-folia-handoff`.
- Claimed from `main` `c1054da6a8f89b312df2e05e25edc958fceda7ef`.
- Status: `ACTIVE` / exact-head validation pending.

## Objective
Remove off-owner live `Player` reads from the reported Cheat Tester and fake-base global-to-entity handoffs while preserving durable tester recovery when a target is unavailable or its entity scheduler retires/rejects work.

## Scope
- `paper/src/main/java/net/enthusia/staff/paper/tester/CheatTesterManager.java`
- `paper/src/main/java/net/enthusia/staff/paper/tester/CheatTesterRuntimeSupport.java`
- `paper/src/main/java/net/enthusia/staff/paper/tester/FakeBaseManager.java`
- package-local Folia handoff helper and focused tests under the same tester package.
- This unique package-state file only. Shared registry/workspace/handoff files remain untouched while T01 owns them.

## Exclusions and ownership
No migrations, schema changes, Discord code, staff random-teleport code, X03/D08/D09/D13 work, provider/cutover behavior, or production deployment. Merged migration ceiling at claim is V20; X03 retains V21 and D09 retains V22.

## Repair contract
Global scheduler work may resolve a `Player` reference only. Availability/state reads and product continuations run on `Player#getScheduler()`. Scheduler rejection, retirement, and dispatch exceptions settle the unavailable path at most once. Mutating tester recovery allocates no runtime session/asset lock until the entity-owner callback runs; when no callback can run, the durable journal row remains authoritative for later recovery.

## Tests and validation
- Behavioral handoff tests cover owner-only availability reads, offline rejection, scheduler rejection, duplicate/late retirement, duplicate execution, and scheduler exceptions.
- Wiring regression covers the exact reported manager/runtime/fake-base bridges.
- Java 21 focused tests: PASS locally.
- Java 21 full `:paper:test`: PASS locally.
- Lizard changed-code gate (`CCN <= 8`, method length `<= 50`, args `<= 8`): PASS, zero warnings.
- Local root `clean build`: NOT PASSING EVIDENCE; Testcontainers initialization is blocked because the workstation has no usable Docker environment.
- `git diff --check`, hosted exact-head build/integration, Codacy, review reconciliation, runtime artifact integrity, and the finding-required representative real Folia acceptance remain required before merge.

## Completion
One normal-merge PR to `main`; no squash/rebase/force-push/auto-merge. Merge only after every applicable exact-head gate is genuinely green, then verify containment/current `main` and publish `FIXED R01-004` evidence in issue #216.
