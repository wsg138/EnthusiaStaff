# ES-F01004 — Cheat Tester Folia player handoff repair

## Identity and status
- Finding: `R01-004` in issue `#216`.
- Branch: `package/es-f01004-cheat-tester-folia-handoff`.
- PR: `#221`.
- Claimed from `main` `c1054da6a8f89b312df2e05e25edc958fceda7ef`.
- Frozen validated product head: `4eab447191ee7a379328ad8d5e7c36ad755e046d`.
- Reconciled live `main`: `313add94027d16bcfe08529136972fe49f6746f5`.
- Status: `BLOCKED` / `PARKED_BLOCKED` — representative real Folia acceptance is unavailable.
- Recovery handoff: `ai-agents/reports/package-handoffs/2026-09-20-es-f01004-folia-staging-blocked.md`.

## Objective
Remove off-owner live `Player` reads from the reported Cheat Tester and fake-base global-to-entity handoffs while preserving durable tester recovery when a target is unavailable or its entity scheduler retires/rejects work.

## Scope
- `paper/src/main/java/net/enthusia/staff/paper/tester/CheatTesterManager.java`
- `paper/src/main/java/net/enthusia/staff/paper/tester/CheatTesterRuntimeSupport.java`
- `paper/src/main/java/net/enthusia/staff/paper/tester/FakeBaseManager.java`
- package-local Folia handoff helper and focused tests under the same tester package.
- This unique package-state file and package-local recovery handoff only. Shared registry/workspace/latest-handoff files remain untouched while T01 owns them and the owner instruction forbids a replacement branch/PR.

## Exclusions and ownership
No migrations, schema changes, Discord code, staff random-teleport code, X03/D08/D09/D13 work, provider/cutover behavior, or production deployment. Merged migration ceiling at claim is V20; X03 retains V21 and D09 retains V22. Live `main` advanced after claim through PR #218 in punishment-request Folia paths and PR #217 in StaffBot mute-ownership paths; neither touches tester paths or collides with R01-004.

## Repair contract
Global scheduler work may resolve a `Player` reference only. Availability/state reads and product continuations run on `Player#getScheduler()`. Scheduler rejection, retirement, global/entity dispatch exceptions, duplicate execution, and duplicate/late retirement settle the handoff at most once. Mutating tester recovery allocates no runtime session/asset lock until the entity-owner callback runs; when no callback can run, the durable journal row remains authoritative for later recovery. Restoration re-resolves the target by UUID after worker-side evidence checkpointing rather than retaining a stale `Player`.

## Verified implementation behavior
Independent recovery review confirmed the proposed handoff handles:
- owner-only `Player` availability/state reads;
- offline players;
- entity retirement and scheduler rejection;
- duplicate execution and duplicate/late retirement callbacks;
- scheduler exceptions;
- restart/recovery of durable Cheat Tester state;
- fake-base handoffs;
- stale-`Player` avoidance across worker boundaries;
- success/failure settlement exactly once through the handoff plus existing session/journal fencing.

No product rewrite was made during recovery because the existing implementation is correct within the finding boundary.

## Tests and validation for frozen product head `4eab447191ee7a379328ad8d5e7c36ad755e046d`
- Behavioral handoff tests cover owner-only availability reads, offline rejection, scheduler rejection, duplicate/late retirement, duplicate execution, and scheduler exceptions: PASS on Java 21.
- Wiring regression covers the exact reported manager/runtime/fake-base bridges: PASS on Java 21.
- Independent Java 21 full `:paper:test --rerun-tasks --no-build-cache`: PASS.
- Existing journal/session tests cover durable restart/reuse, completion conflict/retry, cancellation/journal races, duplicate finishing, and failed-submission rollback.
- Hosted exact-product-head Coverage run `35537363011`: PASS; executed `clean build jacocoAggregateReport runtimeJars` on Java 21.
- Hosted `java-21-validation` artifact `10613299305`: digest `sha256:b5eb129a60d84d619c53b6d702d91d7ba3ada4dd64991c18605d852bbf655bd6`.
- Hosted Sentinel artifact build run `35537362980`: PASS on the exact product head.
- Sentinel Paper artifact `10612739527`: digest `sha256:1b7aa494bd7378a2c5efcd1c0d137cccb445c96893efaa041bbfc30a6df74041`.
- Authority bridge artifact `10613283727`: digest `sha256:c99f9c565ee5a313485245f3cdeb131f1ccff5bc559ffa0c089eb1a951963dc9`.
- Sentinel exact-SHA restart job `473`: PASS with terminal `PAPER_RESTART_OK`; Paper reached readiness and stopped cleanly twice against one disposable state. Source comment `5752881805`; status comment `5752885142`.
- Codacy: PASS with 0 new valid issues; reported PR complexity metric 6.
- Lizard changed-production-code gate (`CCN <= 8`, method length `<= 50`, args <= 8): PASS, zero warnings.
- `git diff --check origin/main...HEAD`: PASS.
- Review reconciliation: no unresolved human review threads, requested changes, or valid static-analysis findings. CodeRabbit did not auto-review this repository and is not counted as evidence.
- Local repository-wide clean build is not separately claimed because the workstation has no usable Docker command; the successful hosted exact-head clean build supplies the executable integration evidence.

## Genuine blocker
The R01-004 finding explicitly requires representative real Folia acceptance. The repository currently has no Folia staging profile: `.enthusia-test.yml` exposes only the Paper `restart` profile. Current requirements/status documentation also records representative Folia staging as unavailable/staging-pending for staff tools and cheat testers. The successful Paper/Sentinel restart does not prove strict Folia entity ownership.

Unavailable, skipped, stale, or infrastructure-failed evidence is not a pass. Therefore PR #221 must remain open and unmerged.

## Exact unblock condition
Provision or expose an owner-approved representative Folia staging environment, then exercise Cheat Tester start, finish/restoration, durable restart/recovery, unavailable/retired target handling, and fake-base handoffs on a current candidate derived from frozen product head `4eab447191ee7a379328ad8d5e7c36ad755e046d`. Require a genuine terminal Folia pass. Then reconcile live `main` and all package collisions, synchronize only as required, rerun invalidated exact-head gates, and merge PR #221 with a normal merge commit only.

## Completion
Do not publish `FIXED R01-004` until the required Folia acceptance passes, PR #221 merges normally, and containment on current `main` is verified. Do not select another finding from this worker.