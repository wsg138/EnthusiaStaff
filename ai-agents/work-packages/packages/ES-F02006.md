# ES-F02006 — Case-wide sanction hierarchy enforcement

## 1. Package identity
`ES-F02006`; owner-authorized issue #216 repair for finding `R02-006`; internal persistence/domain boundary repair; parallel-safe only while the case-wide sanction mutation store and focused new regression test remain disjoint from active package work.

## 2. Status
`ACTIVE` / `ACTIONABLE_CONTINUATION`.

## 3. Objective
Prevent lower-ranked staff from mutating case-wide sanctions issued by protected higher ranks or SYSTEM through the compatibility commands/GUI path.

## 4. Why the package exists
Issue #216 finding `R02-006` proved that `JdbcSanctionMutationStore.apply(...)` locks the case but never loads `cases.actor_rank` or applies `StaffHierarchy.mayMutate(...)`. The exact-sanction store already enforces this hierarchy under its locked transaction, so the case-wide path could bypass the repository rank-protection model.

## 5. Included finding
`R02-006` only.

## 6. Included behavior
Load the persisted case issuer rank as part of the existing `FOR UPDATE` case lock; parse legacy `OWNER` as `FOUNDER` and unknown/blank ranks fail-closed as `SYSTEM`; reject `HIERARCHY_DENIED` before any case/sanction/event/audit/outbox mutation when `StaffHierarchy` disallows the actor; preserve existing expectation, transaction, replay, and mutation behavior for authorized requests.

## 7. Explicit exclusions
No GUI/command presentation edits, no new bypass capability, no authorization-policy changes, no schema or migration changes, no exact-sanction behavior changes, no D08/D09/D13/X03/T01 work, no deployment or authority/cutover changes.

## 8. Dependencies
Uses existing `cases.actor_rank`, `StaffHierarchy`, and current case-wide sanction request actor rank. No new external dependency.

## 9. Component and repository boundaries
Only `wsg138/EnthusiaStaff`; executable changes are limited to `persistence/.../JdbcSanctionMutationStore.java` and focused `integration-tests/.../CaseSanctionHierarchyIntegrationTest.java` unless validation discovers a same-finding blocker.

## 10. Required branch
`package/es-f02006-case-sanction-hierarchy` from exact start `c1054da6a8f89b312df2e05e25edc958fceda7ef`.

## 11. Required PR
One normal-merge PR to `EnthusiaStaff:main`.

## 12. Implementation checklist
- [x] Reconcile live main, issue #216, open PRs/branches, package ownership, validation state, and migration reservations.
- [x] Atomically claim `R02-006` in issue #216 and re-read the issue; no competing earlier claim exists.
- [x] Reconstruct the missing hierarchy check on current main.
- [x] Create the dedicated branch from exact main.
- [x] Implement hierarchy enforcement using the issuer rank read under the existing case row lock.
- [x] Add behavioral MariaDB coverage for allowed, denied, retry/restart, stale expectation, rollback/no-side-effect, and concurrent issuer-rank change paths.
- [ ] Run focused/full exact-head Java 21 validation and analyzers.
- [ ] Open/review the PR, resolve all valid findings, and require Codacy zero new valid findings.
- [ ] Reconcile current main/concurrent ownership and merge normally only if every required gate passes.
- [ ] Verify containment and publish terminal package/issue state.

## 13. Acceptance criteria
Case-wide mutation uses persisted issuer rank under the same case lock as the mutation; Mod may mutate Helper/Mod-issued cases; Mod->Admin/Founder, Admin->Founder, and any actor->SYSTEM reject; rejected requests produce no durable mutation side effects; authorized replay remains idempotent; rejected retries can re-evaluate changed persisted rank after restart; stale expectations remain independently enforced.

## 14. Test requirements
Focused MariaDB integration tests plus the repository's applicable Java 21 clean build/tests, changed-code CCN/method-length checks, PMD/static analysis, `git diff --check`, hosted checks, Codacy, and review on the exact final executable head. Runtime/artifact, Sentinel/Pi, and staging gates apply only if current repository policy/configuration makes them applicable to this persistence-only scope.

## 15. Static-analysis requirements
Zero new valid Codacy/PMD findings. No broad suppressions or analyzer weakening.

## 16. Documentation requirements
This unique package record and handoff may be written while T01 owns the shared canonical registry/workspace/latest-handoff files. Shared canonical state must be serialized later rather than overwritten concurrently.

## 17. Security and privacy requirements
No production moderation data, player identifiers, credentials, or private case content is published. Tests use synthetic UUIDs/case IDs and reasons.

## 18. Migration impact
None. Main remains through V20; X03 owns branch-local V21 and D09 owns branch-local V22.

## 19. Java/Bedrock considerations
No identity-format change. The hierarchy decision uses staff rank and persisted case issuer rank only.

## 20. Distributed/runtime considerations
The issuer rank is selected in the existing case `SELECT ... FOR UPDATE`, so a concurrent rank update must serialize with the sanction mutation. No scheduler behavior is changed.

## 21. External-provider considerations
None.

## 22. Completion definition
Exact final head passes every applicable build/test/static/review gate, PR merges with a normal merge commit, exact approved head is contained, no unintended paths are absorbed, canonical terminal state is published collision-safely, and issue #216 receives `FIXED R02-006` evidence.

## 23. Resume state
Worker: owner-authorized issue #216 parallel fixer slot 2. Branch: `package/es-f02006-case-sanction-hierarchy`. Start main: `c1054da6a8f89b312df2e05e25edc958fceda7ef`. Claim comment: issue #216 comment `5752550578`.

## 24. Last completed checkpoint
Product repair and focused regression coverage prepared for one coherent branch commit. Exact-head validation has not yet been claimed as passing.

## 25. Remaining checklist
Commit checkpoint, validate, review, Codacy/hosted gates, final collision reconciliation, normal merge, containment, canonical terminal publication, issue `FIXED` comment.

## 26. Known blockers
Shared canonical orchestration files are currently owned by T01 / PR #215; do not overwrite them. This is a serialization constraint for package-state publication, not a product-path collision.

## 27. Final evidence
Pending.

## 28. Merge and synchronization record
Pending. Internal package; external parity not applicable.
