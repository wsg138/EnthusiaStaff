# ES-F09002 — Durable mute-role ownership audit lookup

## 1. Package identity
`ES-F09002`; owner-authorized issue #216 repair for finding `R09-002`; internal `COMP-DISCORD`; parallel-safe only while D07 mute-ownership paths remain disjoint from active package work.

## 2. Status
`ACTIVE` / `ACTIONABLE_CONTINUATION`.

## 3. Objective
Prevent long-lived managed Discord mutes from becoming permanently unremovable when their original D07-owned role assignment falls outside the newest 100 guild-wide role-update audit entries.

## 4. Why the package exists
Issue #216 finding `R09-002` proved that `JdaMuteRoleOwnership.requireOwnedCurrentRole(...)` retrieved only one 100-entry audit page. After enough unrelated role traffic, the owned assignment disappeared from that page and every reconciliation/removal retry failed with `MUTE_ROLE_OWNERSHIP_UNVERIFIED` even though no relevant target/role change occurred.

## 5. Included finding
`R09-002` only.

## 6. Included behavior
Search Discord role-update audit history newest-to-oldest across JDA pagination until the punishment issuance boundary; stop at the newest relevant target/managed-role change; accept only an exact D07 marker written by this bot for this punishment; fail closed on a newer external relevant change or when no owned assignment can be proven.

## 7. Explicit exclusions
No punishment schema/persistence changes, migrations, D08 cross-platform behavior, D09 investigations, D13 role sync, X03 Market work, public DTO changes, unrelated Discord moderation flows, production deployment, or authority/cutover changes.

## 8. Dependencies
D07 is complete. Owner authorization in issue #216 permits this dedicated repair while parked/active package ownership remains disjoint.

## 9. Component and repository boundaries
Only `wsg138/EnthusiaStaff`; executable changes are limited to `staff-bot/.../JdaMuteRoleOwnership.java` and focused `JdaDiscordPunishmentGatewayTest.java` coverage unless validation discovers a same-finding blocker.

## 10. Required branch
`package/es-f09002-mute-role-ownership-pagination` from exact start `c1054da6a8f89b312df2e05e25edc958fceda7ef`.

## 11. Required PR
One normal-merge PR to `EnthusiaStaff:main`.

## 12. Implementation checklist
- [x] Reconcile live main, issue #216, open PRs/branches, package ownership, validation state, and migration reservations.
- [x] Atomically claim `R09-002` in issue #216 and re-read the issue; no competing earlier claim exists.
- [x] Reconstruct the one-page audit lookup failure on current main.
- [x] Create the dedicated branch from exact main.
- [x] Implement bounded-by-issuance paginated ownership lookup without adopting external role assignments.
- [x] Add behavioral regression coverage for 100+ unrelated newer audit entries and a newer external relevant change.
- [ ] Run focused/full exact-head Java 21 validation and analyzers.
- [ ] Open/review the PR, resolve all valid findings, and require Codacy zero new valid findings.
- [ ] Reconcile current main/concurrent ownership and merge normally only if every required gate passes.
- [ ] Verify containment and publish terminal package/issue state.

## 13. Acceptance criteria
A D07-owned managed mute remains provable after more than one audit page of unrelated role updates; the latest relevant target/role change remains authoritative; externally assigned roles are never adopted; audit/persistence failures remain retryable through existing gateway classification; no unrelated active package path is modified.

## 14. Test requirements
Focused staff-bot regression tests plus the repository's applicable Java 21 clean build/tests, runtime-JAR verification, changed-code CCN/method-length checks, PMD/static analysis, `git diff --check`, hosted checks, Codacy, and review on the exact final executable head. Sentinel/Pi/staging apply only if current policy/configuration makes them applicable to this scope.

## 15. Static-analysis requirements
Zero new valid Codacy/PMD findings. No broad suppressions or analyzer weakening.

## 16. Documentation requirements
This unique package record and handoff may be written while T01 owns the shared canonical registry/workspace/latest-handoff files. Shared canonical state must be serialized later rather than overwritten concurrently.

## 17. Security and privacy requirements
Do not persist or publish raw Discord audit content, member data, credentials, private messages, production identifiers, or secrets. Tests use synthetic identifiers only.

## 18. Migration impact
None. Main remains through V20; X03 owns branch-local V21 and D09 owns branch-local V22.

## 19. Java/Bedrock considerations
Not applicable to identity representation; this package changes only Discord managed-role ownership proof.

## 20. Distributed/runtime considerations
JDA audit retrieval remains blocking only on the existing dedicated D07 worker path. Pagination is short-circuited at the newest relevant change and bounded by punishment issuance time plus the existing one-minute skew.

## 21. External-provider considerations
Discord/JDA only; no external repository copy or parity requirement.

## 22. Completion definition
Exact final head passes every applicable build/test/static/review/runtime gate, PR merges with a normal merge commit, exact approved head is contained, no unintended paths are absorbed, canonical terminal state is published collision-safely, and issue #216 receives `FIXED R09-002` evidence.

## 23. Resume state
Worker: owner-authorized issue #216 fixer slot 3. Branch: `package/es-f09002-mute-role-ownership-pagination`. Start main: `c1054da6a8f89b312df2e05e25edc958fceda7ef`. Claim comment: issue #216 comment `5752515741`.

## 24. Last completed checkpoint
Product repair and focused regression tests are committed on the package branch. Exact-head validation has not yet been claimed as passing.

## 25. Remaining checklist
Validate, review, Codacy/hosted gates, final collision reconciliation, normal merge, containment, canonical terminal publication, issue `FIXED` comment.

## 26. Known blockers
Shared canonical orchestration files are currently owned by T01 / PR #215; do not overwrite them. This is a serialization constraint for package-state publication, not a product-path collision.

## 27. Final evidence
Pending.

## 28. Merge and synchronization record
Pending. Internal package; external parity not applicable.
