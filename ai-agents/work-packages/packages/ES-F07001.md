# ES-F07001 ? Network presence inheritance fence

## 1. Package identity
`ES-F07001`; owner-authorized issue #216 repair for finding `R07-001`; internal persistence concurrency repair.

## 2. Status
`ACTIVE` / `ACTIONABLE_CONTINUATION`.

## 3. Objective
Prevent a network-identity observation from automatically inheriting a sanction using stale offline presence after the related source account has concurrently come online.

## 4. Included finding
`R07-001` only.

## 5. Included behavior
Candidate discovery remains bounded; the joining player and every matched source-player row are then locked in deterministic UUID-byte order. Presence used for inheritance is derived from the locked rows while conservatively retaining any online state observed during candidate discovery. The source presence rows remain locked through the relationship/evidence/sanction decision.

## 6. Explicit exclusions
No Velocity lifecycle edits, no `JdbcPlayerDirectory` behavior changes, no schema or migration changes, no new retry framework, no unrelated network-identity policy changes, no X03/D08/D09/D13/T01 or other fixer work.

## 7. Component and repository boundaries
Only `wsg138/EnthusiaStaff`; executable changes are limited to `JdbcNetworkIdentityStore.java` and focused MariaDB integration coverage unless validation exposes a same-finding blocker.

## 8. Required branch
`package/es-f07001-network-presence-fence` from exact start `c1054da6a8f89b312df2e05e25edc958fceda7ef`.

## 9. Required PR
One normal-merge PR to `EnthusiaStaff:main`.

## 10. Implementation checklist
- [x] Reconcile live main, issue #216, open PRs/branches, package ownership, migration reservations, and current fixer claims.
- [x] Claim `R07-001` in issue #216 and re-read the queue; no competing claim exists.
- [x] Reconstruct the stale-presence TOCTOU on current main.
- [x] Create an isolated dedicated branch/worktree from exact main without touching the dirty pre-existing checkout.
- [x] Implement deterministic player-row locking and conservative presence reconciliation.
- [x] Add behavioral coverage for stable offline inheritance, offline?online TOCTOU, online?offline fail-safe behavior, and opposite-direction lock ordering.
- [x] Java 21 production/test compilation passes locally.
- [x] `:persistence:test :domain:test` pass locally.
- [x] Changed production methods stay within CCN <= 8, length <= 50, args <= 8; no new Lizard warning.
- [x] `git diff --check` passes locally.
- [ ] Execute MariaDB/Testcontainers regression on hosted infrastructure (local Docker unavailable; local initialization failed before any test ran).
- [ ] Run exact-head hosted build/tests/static/Codacy/review gates.
- [ ] Reconcile live main/concurrent ownership and merge normally only if every required gate passes.
- [ ] Verify containment and publish terminal package/issue state.

## 11. Acceptance criteria
A source going offline?online after candidate discovery cannot be treated as offline for automatic inheritance; online?offline in the same window remains conservatively fail-safe; stable offline single-account evidence can still inherit exactly once; source presence is stable during the mutation transaction; inverse concurrent observations use one deterministic player-row lock order; existing manual relationship and durable idempotency rules remain unchanged.

## 12. Test requirements
Focused MariaDB integration coverage plus Java 21 clean applicable build/tests, changed-code complexity/method-length checks, static analysis, `git diff --check`, hosted checks, Codacy zero new valid findings, and zero unresolved valid review findings on the exact final head. Sentinel/Pi/staging apply only if live repository policy makes them applicable to this persistence-only repair.

## 13. Static-analysis requirements
Zero new valid findings. No broad exclusions or analyzer weakening.

## 14. Documentation requirements
This unique package record and handoff are branch-local while T01 / PR #215 owns shared canonical registry/workspace/latest-handoff files. Shared canonical publication must serialize later rather than overwrite concurrent ownership.

## 15. Security and privacy requirements
No production player/network data, raw addresses, credentials, or private evidence. Tests use synthetic UUIDs and token bytes only.

## 16. Migration impact
None. Main remains through V20; X03 owns branch-local V21 and D09 owns branch-local V22.

## 17. Distributed/runtime considerations
The persistence transaction serializes the source presence read against independent `JdbcPlayerDirectory` writes without moving blocking database work onto Velocity/Paper event threads.

## 18. Completion definition
Exact final head passes every applicable executable/static/review gate, the PR merges with a normal merge commit, approved head containment and path scope are verified, canonical terminal state is published collision-safely, and issue #216 receives `FIXED R07-001` evidence.

## 19. Resume state
Worker: owner-authorized issue #216 parallel fixer slot 4. Claim comment: issue #216 comment `5752623651`. Branch: `package/es-f07001-network-presence-fence`. Start main: `c1054da6a8f89b312df2e05e25edc958fceda7ef`.

## 20. Current blocker/evidence state
Local Docker/Testcontainers execution is unavailable and is not a pass. Hosted Testcontainers and all exact-head hosted/static/review gates remain mandatory before merge. Shared canonical package-state files remain serialization-owned by T01 / PR #215.
