# ES-F09001 — Discord kick retry ambiguity

## 1. Package identity
`ES-F09001`; owner-authorized issue #216 repair for finding `R09-001`; internal `COMP-DISCORD` / D07 punishment delivery.

## 2. Status
`ACTIVE` / `IMPLEMENTING`.

## 3. Objective
Prevent automatic Discord KICK retries from issuing a second destructive kick when the prior attempt may already have crossed the Discord effect boundary, while preserving a safe retry path when a failure is durably known to have occurred before kick dispatch.

## 4. Included finding
`R09-001` only.

## 5. Included behavior
- Persist a distinct pre-effect retry marker for kick failures that are safe to retry.
- Treat reclaimed or otherwise unproven later kick attempts as verification-only unless durable state proves the prior attempt ended before dispatch.
- Convert failures around the destructive JDA kick request into an ambiguous durable result rather than a blindly retryable transport error.
- Use the existing D07 kick audit marker to prove a prior kick before settling it as applied.
- Bound ambiguous ownership verification; never loop indefinitely and never claim unknown state is successful.
- Preserve existing non-kick punishment retry behavior.

## 6. Explicit exclusions
No R09-002/003/004/005 changes, no mute-role ownership changes, no D08/D09/D13 work, no public API expansion, no schema migration, no production deployment, and no shared canonical orchestration edits while T01 owns those paths.

## 7. Branch and claim
Branch: `package/es-f09001-discord-kick-retry-ambiguity` from exact main `d9a11199eb11f47ef255e87a34761363a905e86c`.
Issue #216 claim: `CLAIM R09-001 by ES-F09001/package/es-f09001-discord-kick-retry-ambiguity`.

## 8. Product paths
- `staff-bot/src/main/java/net/enthusia/staff/discordbot/DiscordKickRetryPolicy.java`
- `staff-bot/src/main/java/net/enthusia/staff/discordbot/DiscordPunishmentWorker.java`
- `staff-bot/src/main/java/net/enthusia/staff/discordbot/JdaDiscordPunishmentGateway.java`
- `staff-bot/src/main/java/net/enthusia/staff/discordbot/JdaKickEnforcer.java`

## 9. Test paths
- `staff-bot/src/test/java/net/enthusia/staff/discordbot/DiscordKickRetryAmbiguityTest.java`
- `staff-bot/src/test/java/net/enthusia/staff/discordbot/JdaKickEnforcerTest.java`
- `staff-bot/src/test/java/net/enthusia/staff/discordbot/DiscordPunishmentCoordinatorTest.java`
- `integration-tests/src/test/java/net/enthusia/staff/integration/DiscordKickAmbiguityPersistenceIntegrationTest.java`

## 10. Durable semantics
`KICK_PRE_EFFECT_RETRY` means a prior attempt durably settled before any destructive kick dispatch and a later lease may dispatch once. `KICK_RESULT_AMBIGUOUS` means the remote outcome is not proven. An ambiguous/reclaimed attempt may only verify the existing exact D07 audit marker; it may not issue another kick. One additional verification pass is allowed to accommodate delayed audit visibility; unresolved ambiguity then becomes `FAILED_APPLY`, with `externalApplied=false` and no notification falsely claiming success.

## 11. Persistence and migrations
No migration. Existing V19 reconciliation JSON already durably stores punishment state, `lastErrorCode`, `externalApplied`, delivery outcome, transition key, revisions, and work leases. Main is through V20; X03 owns branch-local V21 and D09 owns branch-local V22.

## 12. Concurrency/collision state
Fresh reconciliation found no open PR owning the R09-001 core paths. R09-002 is already merged. T01 / PR #215 owns shared canonical package/workspace/latest-handoff files, so this package must not overwrite them.

## 13. Required evidence
Java 21 clean StaffBot tests and runtime JAR verification; applicable integration tests; `git diff --check`; changed-code Lizard CCN/method-length checks; hosted Coverage and StaffBot workflows; Codacy zero new valid findings; exact-head review/collision reconciliation; applicable non-production runtime evidence.

## 14. Required behavior coverage
Clean kick; definitive pre-effect rejection; safe pre-effect retry; ambiguous dispatch; restart/reclaimed lease; no duplicate destructive effect; lease/revision/replay persistence; durable ambiguity across restart; authorization/target membership fencing; non-kick regression; nonretryable JDA failure; shutdown/queue rejection; truthful durable delivery/error state.

## 15. Completion definition
All exact-head gates pass, current main/path ownership is reconciled again, the PR is merged with a normal merge commit, containment is verified, and issue #216 receives `FIXED R09-001 | PR #... | merge <sha> | evidence ...`. This worker does not self-verify.
